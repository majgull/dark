# The local build binary. `go build ./...` names it after the module
# (`app`, deliberately identical in every repository generated from this
# template), so this one line keeps a build product out of every commit.
/app

# Go well-known
*.exe
*.test
*.out
*.so
*.dylib
vendor/
