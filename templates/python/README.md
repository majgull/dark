# python template (dark/templates)

The starting tree every Python task repo is materialised from: `.dark/verify.sh` (compiles every `.py`, runs every `test_*.py`; green on an empty tree by design), a well-known `.gitignore`, `.editorconfig`. No code skeleton: the task's spec and `start/` overlay shape the code. Hidden acceptance tests never live here.
